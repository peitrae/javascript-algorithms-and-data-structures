class Node {
	constructor(val) {
		this.val = val;
		this.next = null;
		this.prev = null;
	}
}

class DoublyLinkedList {
	constructor() {
		this.head = null;
		this.tail = null;
		this.length = null;
	}

  // push(val)

  // pop()

  // unshift(val)

  // shift()

  // get(index)

  // set(val, index)

  // insert(val, index)

  // remove(index)

  // reverse()

  // toArray()
}


